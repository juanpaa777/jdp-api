// Minimal runtime configuration used by the app and helpful for contributors.
export default {
	port: process.env.PORT ? Number(process.env.PORT) : 3000,
	// Add other config values here (DB URL, JWT secret, etc.) as needed
};
